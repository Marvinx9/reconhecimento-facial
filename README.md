## Reconhecimento Facial com Recognito

criar um ambiente virtual:

```bash
    python -m venv env
```

Ativar ambiente virtual:

```bash
source env/bin/activate
```

Instalação de dependências

```bash
pip install opencv-python && pip install face-recognition
```
